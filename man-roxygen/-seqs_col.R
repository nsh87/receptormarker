#' @param seqs_col Either an integer corresponding to a column index or a string
#'   corresponding to a column name in \code{d} that contains the sequences. If
#'   the data supplied in \code{d} is not a data frame and is instead a
#'   character vector, \code{seqs_col} should be \code{NULL}.
