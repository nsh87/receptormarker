#' @param seqs_col Either an integer corresponding to a column index or a string
#'   corresponding to a column name that contains the sequences. If
#'   the data supplied is not a data frame and is instead a character vector,
#'   \code{seqs_col} should be \code{NULL}.
